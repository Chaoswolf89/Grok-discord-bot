[build]
builder = "RAILPACK"
buildCommand = "pip install -r requirements.txt"

[deploy]
startCommand = "python bot.py"
restartPolicyMaxRetries = 10

# Health Check (Recommended for Discord bots)
[healthcheck]
path = "/"
interval = 30
timeout = 10 